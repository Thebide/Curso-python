import datetime

from constants import MES
from data import balancedb,fmes_ingresos,fmes_gastos,fmes_inversiones
''' 
Creación de matrices financieras (ingresos, gastos, inversiones)
matriz = id (autogenerado?), dato(int), dia(int),  mes(diccionario), año(int)  
operaciones = sumar u restar, eliminar N° de operación seleccionada
'''

def add_ingresos():
  """  monto = input("Ingrese monto:")
    if monto !=0:
        id +=1
    fmes_ingresos = {    }
    ARREGLAR
"""
def delete_ingresos():
    for index, ingreso in enumerate(fmes_ingresos, start =1):
        pass

def add_gastos():
    matriz_gastos = {}

def delete_gastos():
    matriz_gastos = {}

def add_inversiones():
    matriz_inversiones = {}

def delete_inversiones():
    matriz_inversiones = {}

def status():
    pass